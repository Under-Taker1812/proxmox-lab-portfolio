# 11-gitlab-harbor.yml — TODO skeleton (to be written up once done by hand).
# Target pipeline: GitLab CE (VM) → Runner (pod) → build → Harbor (Helm) → Trivy scan → Rancher deploy.
#
# When implemented:
#   - GitLab CE: clone a role=general VM → install gitlab-ce → external_url via sslip.io
#   - Harbor: helm repo add harbor https://helm.goharbor.io → install with expose.type=ingress
#   - Runner: helm install gitlab-runner executor=kubernetes
#   - An ImagePullSecret for Harbor in the workload cluster
---
- name: GitLab + Harbor (not yet implemented)
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Placeholder — not automated yet, still pending manual setup
      ansible.builtin.debug:
        msg: "11-gitlab-harbor.yml is a skeleton. See the roadmap's CI/CD section."
