# 11-gitlab-harbor.yml — TODO skeleton (Hướng 4 — viết SAU khi làm tay xong).
# Pipeline đích: GitLab CE (VM) → Runner (pod) → build → Harbor (Helm) → Trivy scan → Rancher deploy.
#
# Khi triển khai:
#   - GitLab CE: clone VM role=general → install gitlab-ce → external_url sslip.io
#   - Harbor: helm repo add harbor https://helm.goharbor.io → install expose.type=ingress
#   - Runner: helm install gitlab-runner executor=kubernetes
#   - ImagePullSecret cho Harbor trong k8s-prod
---
- name: GitLab + Harbor (chưa triển khai)
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Placeholder — chưa làm tay xong nên chưa automate
      ansible.builtin.debug:
        msg: "11-gitlab-harbor.yml là skeleton. Xem roadmap Hướng 4 + Phase 1-5."
