# A dedicated resolv.conf for RKE2 — never 127.0.0.53 (systemd-resolved's loopback, meaningless from inside a pod). See docs/troubleshooting/networking-and-cni.md Bug #1.
{% for ns in net_dns_upstream %}
nameserver {{ ns }}
{% endfor %}
