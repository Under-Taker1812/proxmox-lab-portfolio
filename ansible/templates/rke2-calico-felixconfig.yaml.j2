# No longer applied as a raw manifest staged before join — that caused an ownership conflict with
# Helm (see docs/troubleshooting/networking-and-cni.md). Kept only for reference on what the spec
# looks like — the real value is now applied via `kubectl patch` AFTER Calico is already installed
# (see 07-join-cluster.yml).
apiVersion: crd.projectcalico.org/v1
kind: FelixConfiguration
metadata:
  name: default
spec:
  iptablesBackend: Legacy
  logSeverityScreen: Info
