# Secret keyring cho ceph-csi. userKey lấy TRỰC TIẾP từ `ceph auth get-key` (tránh typo l/I — bài học C.1).
apiVersion: v1
kind: Secret
metadata:
  name: csi-rbd-secret
  namespace: ceph-csi-rbd
type: Opaque
stringData:
  userID: "{{ ceph_csi_user }}"
  userKey: "{{ ceph_csi_user_key }}"
